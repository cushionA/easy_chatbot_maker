// Day2-4 [自分-A] Occurrence[] → DayStat[]（term × day × locale の mentions / distinctSources / share）
// ヒントのみ:
//   - Map のキーは `${termSlug}|${day}|${locale}`
//   - distinctSources は Set<Source> のサイズ
//   - share = mentions ÷ その `${day}|${locale}` の総 mentions（2 パス目で計算）
// 書き終えてから design/sprint1/refs/aggregate.ref.ts と見比べる（先に見ない）。
export {};
